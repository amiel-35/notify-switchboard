name: Test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  pytest:
    name: Pytest (HA ${{ matrix.ha-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - ha-version: pinned
            pre: false
          - ha-version: dev
            pre: true
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dev dependencies
        run: pip install -r requirements_dev.txt

      - name: Install latest Home Assistant (dev matrix leg)
        if: matrix.pre
        run: pip install --pre --upgrade homeassistant

      - name: Run tests with coverage
        run: pytest --cov=custom_components.notify_switchboard --cov-report=xml

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.ha-version }}
          path: coverage.xml
