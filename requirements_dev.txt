# Keep pytest-homeassistant-custom-component's version aligned with the
# `homeassistant` version below; check its release notes when bumping either.
homeassistant>=2026.9.0
pytest-homeassistant-custom-component>=0.13.246
pytest-cov>=5.0.0
ruff>=0.8.0
mypy>=1.13.0
